events {
}

http {
    server {
        location / {
            root /usr/share/nginx/html;
        }
        location /api/ {
            proxy_pass https://docapp-prod.herokuapp.com/api/;
        }
    }
}
